import { SetEndDateTimePipe } from './set-end-date-time.pipe';

describe('SetEndDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SetEndDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
