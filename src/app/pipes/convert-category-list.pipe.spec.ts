import { ConvertCategoryListPipe } from './convert-category-list.pipe';

describe('ConvertCategoryListPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCategoryListPipe();
    expect(pipe).toBeTruthy();
  });
});
